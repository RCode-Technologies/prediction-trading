# `discovery_summary` — no candidates

Sent when `candidates_passing_min_edge == 0`.

```
🔍 *Research window* · `<mode>`
No bettable candidates passed today.

📋 Watchlist *<N>*
Top leads:
  • <lead 1>
  • <lead 2>
  • <lead 3>
```

Drop the bullet list if there are no leads either.
