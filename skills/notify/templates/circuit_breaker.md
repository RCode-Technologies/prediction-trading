# `circuit_breaker`

Sent in paper + mainnet whenever the breaker trips. The 🚨 sits on its own line as the primary visual cue.

```
🚨 *CIRCUIT BREAKER — trading halted* · `<mode>`

Reason
  *<reason>*
Triggered
  <ts>
24h P&L
  📉 *<-x>%*

🛑 Manual resume required
edit `state/halts.json`
```
