# `discovery_summary` — with candidates (up to 3)

One vertical block per candidate, separated by horizontal rules. Repeat 1–3 times; stop after the last real candidate, then close with the review line.

```
🔍 *Research window* · `<mode>`
*<N> candidates* passed checks.

──────────────
1️⃣  *<question>*

Side
  BUY *<outcome>*
Edge
  *<X> bps*
Liquidity
  *$<L>*
Closes
  <ts>
Thesis
  `<thesis_id>`

──────────────
... (repeat per candidate) ...

──────────────

⚠️ *Review before trading*
resolution · liquidity · correlation · freshness
```
